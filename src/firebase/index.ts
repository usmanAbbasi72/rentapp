'use client';

import { firebaseConfig } from './config';
import { FirebaseClientProvider, useFirebase, useAuth, useFirestore } from './client-provider';

export {
  firebaseConfig,
  FirebaseClientProvider,
  useFirebase,
  useAuth,
  useFirestore,
};
